<UserControl x:Class="Mobius.Views.Pages.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource H1}" Margin="0,0,0,14" Text="Настройки"/>

        <Border Grid.Row="1"
                Background="{StaticResource BgSurfaceBrush}"
                CornerRadius="18"
                Padding="16">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Microphone -->
                <TextBlock Text="Микрофон" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                <ComboBox Grid.Row="1"
                          ItemsSource="{Binding Microphones}"
                          SelectedItem="{Binding SelectedMicrophone}"
                          DisplayMemberPath="Name"
                          Margin="0,0,0,16"/>

                <!-- Save folder -->
                <TextBlock Grid.Row="2" Text="Папка сохранений" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                <Grid Grid.Row="3" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding SaveFolder}" IsReadOnly="True"/>
                    <Button Grid.Column="1" Style="{StaticResource IconButton}" Width="64" Margin="10,0,0,0" Content="…" Command="{Binding ChooseSaveFolderCommand}"/>
                </Grid>

                <!-- Debug -->
                <CheckBox Grid.Row="4"
                          Content="Режим отладки (лог-панель справа)"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          IsChecked="{Binding DebugEnabled}"
                          Margin="2,0,0,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Style="{StaticResource IconButton}"
                    Background="{StaticResource GrayDarkBrush}"
                    Content="←"
                    ToolTip="Назад"
                    Command="{Binding BackCommand}"
                    Margin="0,0,10,0"/>

            <Button Grid.Column="1"
                    Style="{StaticResource IconButton}"
                    Background="{StaticResource GreenBrush}"
                    Content="💾"
                    ToolTip="Сохранить"
                    Command="{Binding SaveToFileCommand}"
                    Margin="0,0,10,0"/>

            <Button Grid.Column="3"
                    Style="{StaticResource IconButton}"
                    Background="{StaticResource PurpleBar2Brush}"
                    Content="⤓"
                    ToolTip="Загрузить"
                    Command="{Binding LoadFromFileCommand}"/>
        </Grid>
    </Grid>
</UserControl>
